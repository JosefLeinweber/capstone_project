#include "AudioBuffer.h"
#include "ConnectionManagerThread.h"
#include <catch2/catch_test_macros.hpp>
#include <iostream>
#include <thread>

TEST_CASE("ConnectionManagerThread & ConnectionManagerThread | successfull "
          "connection")
{
    FAIL("Test not implemented");
    // AudioBufferFIFO inputRingBuffer_1(2, 2);
    // AudioBufferFIFO outputRingBuffer_1(2, 2);
    // AudioBufferFIFO inputRingBuffer_2(2, 2);
    // AudioBufferFIFO outputRingBuffer_2(2, 2);

    // cmt_1 = std::make_unique<ConnectionManagerThread>(inputRingBuffer_1,
    //                                                   outputRingBuffer_1);
    // cmt_2 = std::make_unique<ConnectionManagerThread>(inputRingBuffer_2,
    //                                                   outputRingBuffer_2);
}
